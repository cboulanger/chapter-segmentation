# Unredacted public-cache entries written via generate_public_evaluation_cache.py's
# --skip-redaction (2026-08-13 pending-book migration -- these 4 books' redaction
# self-correction couldn't converge without leaking most of the book's prose; cached
# verbatim locally so downstream tooling has something to read, but never committed.
# Each carries "needs_redaction": true -- see evaluation/CLAUDE.md's
# "redaction_overrides.json" section. Remove these lines once real redaction succeeds
# and the corresponding public-cache/*.pages.json is regenerated properly.
public-cache/9781841136400.pages.json
public-cache/9781849463812.pages.json
public-cache/9783789017483.pages.json
public-cache/9788814022272.pages.json
